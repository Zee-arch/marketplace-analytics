data/
warehouse/
.venv/
__pycache__/
.env
.DS_Store
*.duckdb
*.parquet

# working notes for Claude Code sessions -- useful locally for continuity,
# not meant for the public repo (process/session narrative, not deliverable)
CLAUDE.md
SESSION-*.md

# dbt build artifacts -- regenerated by `dbt run`/`dbt test`, not source
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml   # dbt-generated anonymous telemetry ID, not project content
